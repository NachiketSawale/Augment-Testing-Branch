/*
 * $Id$
 * Copyright(c) RIB Software GmbH
 */
//import { ComponentFixture, TestBed } from '@angular/core/testing';

//import { UiMainFrameAppInfoHeaderComponent } from './app-info-header.component';

describe('UiMainFrameAppInfoHeaderComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
/*	let component: UiMainFrameAppInfoHeaderComponent;
	let fixture: ComponentFixture<UiMainFrameAppInfoHeaderComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiMainFrameAppInfoHeaderComponent],
		}).compileComponents();
	});

	beforeEach(() => {
		fixture = TestBed.createComponent(UiMainFrameAppInfoHeaderComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
