/*
 * $Id$
 * Copyright(c) RIB Software GmbH
 */
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UiGridEditorHostComponent } from './editor-host.component';

describe('UiGridEditorHostComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
	/*	let component: UiGridEditorHostComponent;
		let fixture: ComponentFixture<UiGridEditorHostComponent>;

		beforeEach(async () => {
			await TestBed.configureTestingModule({
				declarations: [UiGridEditorHostComponent],
			}).compileComponents();

			fixture = TestBed.createComponent(UiGridEditorHostComponent);
			component = fixture.componentInstance;
			fixture.detectChanges();
		});

		it('should create', () => {
			expect(component).toBeTruthy();
		});*/
});
