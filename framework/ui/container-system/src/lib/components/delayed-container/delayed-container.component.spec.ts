//import { ComponentFixture, TestBed } from '@angular/core/testing';
//import { DelayedContainerComponent } from './delayed-container.component';

describe('DelayedContainerComponent', () => {
// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
	/*
	let component: DelayedContainerComponent;
	let fixture: ComponentFixture<DelayedContainerComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [DelayedContainerComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(DelayedContainerComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
