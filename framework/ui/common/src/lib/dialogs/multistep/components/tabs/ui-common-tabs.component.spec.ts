import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UiCommonTabsComponent } from './ui-common-tabs.component';

describe('UiCommonTabsComponent', () => {
	let component: UiCommonTabsComponent;
	let fixture: ComponentFixture<UiCommonTabsComponent>;
	// TODO: rewrite unit tests or remove file
	// beforeEach(async () => {
	//   await TestBed.configureTestingModule({
	//     imports: [UiCommonTabsComponent]
	//   }).compileComponents();
	//
	//   fixture = TestBed.createComponent(UiCommonTabsComponent);
	//   component = fixture.componentInstance;
	//   fixture.detectChanges();
	// });

	it('should create', () => {
		expect(true).toBeTruthy();
	});
});
