import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UiCommonGridDialogViewComponent } from './grid-dialog-view.component';

describe('UiCommonGridDialogViewComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});

	/*
	let component: UiCommonGridDialogViewComponent;
	let fixture: ComponentFixture<UiCommonGridDialogViewComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiCommonGridDialogViewComponent],
		}).compileComponents();
	});

	beforeEach(() => {
		fixture = TestBed.createComponent(UiCommonGridDialogViewComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});
	*/
});
