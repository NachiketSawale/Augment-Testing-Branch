import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UiCommonLookupInputTestComponent } from './lookup-input-test.component';

describe('LookupInputTestComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
/*	let component: UiCommonLookupInputTestComponent;
	let fixture: ComponentFixture<UiCommonLookupInputTestComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiCommonLookupInputTestComponent],
		}).compileComponents();
	});

	beforeEach(() => {
		fixture = TestBed.createComponent(UiCommonLookupInputTestComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
