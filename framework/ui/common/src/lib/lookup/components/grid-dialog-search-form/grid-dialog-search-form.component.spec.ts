import { ComponentFixture, TestBed } from '@angular/core/testing';

describe('UiCommonGridDialogSearchFormComponent', () => {
	// TODO-DRIZZLE: To be implemented.
	it('is successful', () => {
		expect(true).toBeTruthy();
	});

	/*
	let component: UiCommonGridDialogSearchFormComponent;
	let fixture: ComponentFixture<UiCommonGridDialogSearchFormComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiCommonGridDialogSearchFormComponent],
		}).compileComponents();
	});

	beforeEach(() => {
		fixture = TestBed.createComponent(UiCommonGridDialogSearchFormComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});
	*/
});
