import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UiCommonLookupButtonComponent } from './lookup-button.component';

describe('LookupButtonComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
	/*let component: UiCommonLookupButtonComponent;
	let fixture: ComponentFixture<UiCommonLookupButtonComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiCommonLookupButtonComponent],
		}).compileComponents();
	});

	beforeEach(() => {
		fixture = TestBed.createComponent(UiCommonLookupButtonComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
