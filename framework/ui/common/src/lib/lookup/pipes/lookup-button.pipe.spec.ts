import { UiCommonLookupButtonPipe } from './lookup-button.pipe';

describe('LookupButtonPipe', () => {
	it('create an instance', () => {
		const pipe = new UiCommonLookupButtonPipe();
		expect(pipe).toBeTruthy();
	});
});
