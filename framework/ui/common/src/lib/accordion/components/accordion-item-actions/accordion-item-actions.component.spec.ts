import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UiCommonAccordionItemActionsComponent } from './accordion-item-actions.component';

describe('AccordionItemActionsComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
/*	let component: UiCommonAccordionItemActionsComponent;
	let fixture: ComponentFixture<UiCommonAccordionItemActionsComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiCommonAccordionItemActionsComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(UiCommonAccordionItemActionsComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
