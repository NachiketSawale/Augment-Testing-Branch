/*
 * $Id$
 * Copyright(c) RIB Software GmbH
 */
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UiCommonMenuListSideBarDropdownComponent } from './menu-list-sidebar-dropdown.component';

describe('UiCommonMenuListSideBarDropdownComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
	/*let component: UiCommonMenuListSideBarDropdownComponent;
	let fixture: ComponentFixture<UiCommonMenuListSideBarDropdownComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiCommonMenuListSideBarDropdownComponent],
		}).compileComponents();
	});

	beforeEach(() => {
		fixture = TestBed.createComponent(UiCommonMenuListSideBarDropdownComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
