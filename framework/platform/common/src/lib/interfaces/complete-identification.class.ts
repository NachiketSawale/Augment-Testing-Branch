/**
 * This class is on purpose empty, as complete entities differ on naming and typing of the property related to the main entity
 * the type can be either simple entity or array. Hopefully we can unify this differences in the future.
 * Intention of the class is to bind the entity type in the data service and the complete entity used by the service.
 */


// eslint-disable-next-line @typescript-eslint/no-unused-vars
export abstract class CompleteIdentification<T extends object> {}
