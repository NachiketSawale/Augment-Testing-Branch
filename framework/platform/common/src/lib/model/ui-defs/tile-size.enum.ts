/*
 * $Id$
 * Copyright(c) RIB Software GmbH
 */

/**
 * Represents the size of a desktop tile.
 */
export enum TileSize {

	/**
	 * For small tiles (that occupy half a group column).
	 */
	Small = 0,

	/**
	 * For large tiles (that occupy a full group column).
	 */
	Large = 1
}