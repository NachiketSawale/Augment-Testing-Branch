import { EntityArrayProcessor } from './entity-array-processor.class';

describe('EntityArrayProcessor', () => {
  it('should create an instance', () => {
    expect(new EntityArrayProcessor([])).toBeTruthy();
  });
});
