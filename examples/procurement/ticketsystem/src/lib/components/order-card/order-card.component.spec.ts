import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ProcurementTicketSystemOrderCardComponent } from './order-card.component';

describe('OrderCardComponent', () => {
  // TODO: replace with actual test cases
  it('is successful', () => {
    expect(true).toBeTruthy();
  });
/*
let component: ProcurementTicketSystemOrderCardComponent;
let fixture: ComponentFixture<ProcurementTicketSystemOrderCardComponent>;

beforeEach(async () => {
  await TestBed.configureTestingModule({
    declarations: [ ProcurementTicketSystemOrderCardComponent ]
  })
  .compileComponents();

  fixture = TestBed.createComponent(ProcurementTicketSystemOrderCardComponent);
  component = fixture.componentInstance;
  fixture.detectChanges();
});

it('should create', () => {
  expect(component).toBeTruthy();
});

 */
});
