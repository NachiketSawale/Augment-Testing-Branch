import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ProcurementTicketSystemPlaceOrderFormComponent } from './place-order-form.component';

describe('PlaceOrderFormComponent', () => {
  // TODO: replace with actual test cases
  it('is successful', () => {
    expect(true).toBeTruthy();
  });
  /*
  let component: ProcurementTicketSystemPlaceOrderFormComponent;
  let fixture: ComponentFixture<ProcurementTicketSystemPlaceOrderFormComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ProcurementTicketSystemPlaceOrderFormComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ProcurementTicketSystemPlaceOrderFormComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });

   */
});
