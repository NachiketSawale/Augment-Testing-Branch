/*
 * Copyright(c) RIB Software GmbH
 */

describe('ProcurementPricecomparisonCompareSettingSummaryFieldComponent', () => {
  /*let component: CompareSettingSummaryFieldComponent;
  let fixture: ComponentFixture<CompareSettingSummaryFieldComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [CompareSettingSummaryFieldComponent],
    }).compileComponents();

    fixture = TestBed.createComponent(CompareSettingSummaryFieldComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });*/

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
