/*
 * Copyright(c) RIB Software GmbH
 */

describe('ProcurementPricecomparisonCompareSettingBillingSchemaFieldComponent', () => {
	/*let component: CompareSettingBillingSchemaFieldComponent;
	let fixture: ComponentFixture<CompareSettingBillingSchemaFieldComponent>;

	beforeEach(async () => {
	  await TestBed.configureTestingModule({
		 declarations: [CompareSettingBillingSchemaFieldComponent],
	  }).compileComponents();

	  fixture = TestBed.createComponent(CompareSettingBillingSchemaFieldComponent);
	  component = fixture.componentInstance;
	  fixture.detectChanges();
	});
 */
	it('should create', () => {
		expect(true).toBeTruthy();
	});
});
