/*
 * Copyright(c) RIB Software GmbH
 */

describe('ProcurementPricecomparisonCreateContractWizardContractViewComponent', () => {
	/*let component: CreateContractWizardContractViewComponent;
	let fixture: ComponentFixture<CreateContractWizardContractViewComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [CreateContractWizardContractViewComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(CreateContractWizardContractViewComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/

	it('should create', () => {
		expect(true).toBeTruthy();
	});
});
