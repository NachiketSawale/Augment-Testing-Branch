import { TestBed } from '@angular/core/testing';

import { WorkflowApproverConfigDataService } from './workflow-approver-config-data.service';

describe('WorkflowApproverConfigDataService', () => {
	// let service: WorkflowApproverConfigDataService;

	// beforeEach(() => {
	// 	TestBed.configureTestingModule({});
	// 	service = TestBed.inject(WorkflowApproverConfigDataService);
	// });

	it('should be created', () => {
		expect(true).toBeTruthy();
	});
});
