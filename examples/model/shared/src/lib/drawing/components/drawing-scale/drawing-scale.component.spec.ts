import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ModelSharedDrawingScaleComponent } from './drawing-scale.component';

describe('DrawingScaleComponent', () => {
  // TODO: replace with actual test cases
  it('is successful', () => {
    expect(true).toBeTruthy();
  });

  /*
  let component: ModelSharedDrawingScaleComponent;
  let fixture: ComponentFixture<ModelSharedDrawingScaleComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ModelSharedDrawingScaleComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ModelSharedDrawingScaleComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
   */
});
