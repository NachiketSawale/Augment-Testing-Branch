import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ModelSharedCalibrationComponent } from './calibration.component';

describe('CalibrationComponent', () => {
  // TODO: replace with actual test cases
  it('is successful', () => {
    expect(true).toBeTruthy();
  });

  /*
  let component: ModelSharedCalibrationComponent;
  let fixture: ComponentFixture<ModelSharedCalibrationComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ModelSharedCalibrationComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ModelSharedCalibrationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
   */
});
