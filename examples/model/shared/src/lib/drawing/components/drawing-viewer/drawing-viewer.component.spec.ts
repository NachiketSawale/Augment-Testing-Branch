import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ModelSharedDrawingViewerComponent } from './drawing-viewer.component';

describe('DrawingViewerComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});

	/*
	let component: ModelSharedDrawingViewerComponent;
	let fixture: ComponentFixture<ModelSharedDrawingViewerComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [ModelSharedDrawingViewerComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(ModelSharedDrawingViewerComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});
	 */
});
