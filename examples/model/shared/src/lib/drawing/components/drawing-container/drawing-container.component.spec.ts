import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ModelSharedDrawingContainerComponent } from './drawing-container.component';

describe('ModelSharedDrawingContainerComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});

	/*
	let component: ModelSharedDrawingContainerComponent;
	let fixture: ComponentFixture<ModelSharedDrawingContainerComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [ModelSharedDrawingContainerComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(ModelSharedDrawingContainerComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});
	 */
});
