import { TestBed } from '@angular/core/testing';

//import { PlatformPinningContextService } from './platform-pinning-context.service';

describe('PlatformPinningContextService', () => {
	//   let service: PlatformPinningContextService;

	//   beforeEach(() => {
	//     TestBed.configureTestingModule({});
	//     service = TestBed.inject(PlatformPinningContextService);
	//   });

	it('should be created', () => {
		expect(true).toBeTruthy();
	});
});
