/*
export interface ITranslateObj {
	[x: string]: string | number | boolean | ITile;
}

export interface ITile {
	id: string;
	iconClass: string;
	tileSize: number;
	tileColor: number;
	tileOpacity: number;
	textColor: number;
	iconColor: number;
	routeUrl: string;
	permission: string;
	group?: string;
	displayName?: string;
	description$tr$?: string;
	description?: string;
	displayName$tr$?: string;
}

export interface IItem {
	[x: string]: string | number | boolean | ITile;
}

export interface IOptions {
	// TODO: remove?
	//notFound?: Function | boolean;
	recursive: boolean;
}

export interface IInstant {
	[x: string]: Array<string>;
}

// TODO: remove?
/*
export interface IContext {
	[x: string]: string | {};
}
*/