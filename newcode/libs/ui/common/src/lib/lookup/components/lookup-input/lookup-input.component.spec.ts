import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UiCommonLookupInputComponent } from './lookup-input.component';

describe('LookupInputComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
/*	let component: UiCommonLookupInputComponent;
	let fixture: ComponentFixture<UiCommonLookupInputComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiCommonLookupInputComponent],
		}).compileComponents();
	});

	beforeEach(() => {
		fixture = TestBed.createComponent(UiCommonLookupInputComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
