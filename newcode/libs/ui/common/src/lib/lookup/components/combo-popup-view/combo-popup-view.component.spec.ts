import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UiCommonComboPopupViewComponent } from './combo-popup-view.component';

describe('UiCommonComboPopupViewComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
/*	let component: UiCommonComboPopupViewComponent;
	let fixture: ComponentFixture<UiCommonComboPopupViewComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiCommonComboPopupViewComponent],
		}).compileComponents();
	});

	beforeEach(() => {
		fixture = TestBed.createComponent(UiCommonComboPopupViewComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
