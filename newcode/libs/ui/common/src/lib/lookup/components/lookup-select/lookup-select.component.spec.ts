import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UiCommonLookupSelectComponent } from './lookup-select.component';

describe('LookupSelectComponent', () => {
	let component: UiCommonLookupSelectComponent<string, unknown>;
	let fixture: ComponentFixture<UiCommonLookupSelectComponent<string, unknown>>;

	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});

	/*
	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiCommonLookupSelectComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(UiCommonLookupSelectComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});
	 */
});
