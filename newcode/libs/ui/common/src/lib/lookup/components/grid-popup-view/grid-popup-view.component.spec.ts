import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UiCommonGridPopupViewComponent } from './grid-popup-view.component';

describe('UiCommonGridPopupViewComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});

	/*
	let component: UiCommonGridPopupViewComponent;
	let fixture: ComponentFixture<UiCommonGridPopupViewComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiCommonGridPopupViewComponent],
		}).compileComponents();
	});

	beforeEach(() => {
		fixture = TestBed.createComponent(UiCommonGridPopupViewComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});
	*/
});
