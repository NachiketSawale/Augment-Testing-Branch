/*
 * Copyright(c) RIB Software GmbH
 */

import {ElementRef} from '@angular/core';

/**
 * Common lookup buttons references
 */
export interface ILookupBtnRefs {
    /**
     * Reference to edit button
     */
    editBtn: ElementRef;
    /**
     * Reference to clear button
     */
    clearBtn: ElementRef;
}