//import { ComponentFixture, TestBed } from '@angular/core/testing';
//import { UiCommonPopupMenuComponent } from './popup-menu.component';

describe('PopupMenuComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
	/*let component: UiCommonPopupMenuComponent;
	let fixture: ComponentFixture<UiCommonPopupMenuComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiCommonPopupMenuComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(UiCommonPopupMenuComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
