import { ColourConverterDirective } from './colour-converter.directive';

describe('ColourConverterDirective', () => {
	it('should create an instance', () => {
		// TODO: fix TS2554
		//const directive = new ColourConverterDirective();
		//expect(directive).toBeTruthy();
	});
});
