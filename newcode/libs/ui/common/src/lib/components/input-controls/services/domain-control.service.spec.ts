import { TestBed } from '@angular/core/testing';

import { PlatformDomainControlService } from './domain-control.service';

describe('PlatformDomainControlService', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
/*	let service: PlatformDomainControlService;

	beforeEach(() => {
		TestBed.configureTestingModule({});
		service = TestBed.inject(PlatformDomainControlService);
	});

	it('should be created', () => {
		expect(service).toBeTruthy();
	});*/
});
