import { IbanConverterDirective } from './iban-converter.directive';

describe('IbanConverterDirective', () => {
	it('should create an instance', () => {
		// TODO: fix TS2554
		//const directive = new IbanConverterDirective();
		//expect(directive).toBeTruthy();
	});
});
