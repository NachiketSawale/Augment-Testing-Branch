/*
 * $Id$
 * Copyright(c) RIB Software GmbH
 */
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UiCommonItemListTreeComponent } from './item-list-tree.component';

describe('UiCommonItemListTreeComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
/*	let component: UiCommonItemListTreeComponent;
	let fixture: ComponentFixture<UiCommonItemListTreeComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiCommonItemListTreeComponent],
		}).compileComponents();
	});

	beforeEach(() => {
		fixture = TestBed.createComponent(UiCommonItemListTreeComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});

	it('treeitems should be defined', () => {
		expect(component.treeitems).toBeDefined();
	});

	it('treeitems should not be null ', () => {
		expect(component.treeitems).not.toBeNull();
	});

	it('options should not be null ', () => {
		expect(component.options).not.toBeNull();
	});

	it('searchmodule should not be null ', () => {
		expect(component.searchmodule).not.toBeNull();
	});

	it('searchmodule should be defined ', () => {
		expect(component.searchmodule).toBeDefined();
	});*/
});
