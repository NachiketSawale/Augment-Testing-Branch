import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UiCommonAccordionItemContentComponent } from './accordion-item-content.component';

describe('AccordionItemContentComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
/*	let component: UiCommonAccordionItemContentComponent;
	let fixture: ComponentFixture<UiCommonAccordionItemContentComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiCommonAccordionItemContentComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(UiCommonAccordionItemContentComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
