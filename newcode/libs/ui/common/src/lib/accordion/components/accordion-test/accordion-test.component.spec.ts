import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UiCommonAccordionTestComponent } from './accordion-test.component';

describe('AccordionTestComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
/*	let component: UiCommonAccordionTestComponent;
	let fixture: ComponentFixture<UiCommonAccordionTestComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiCommonAccordionTestComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(UiCommonAccordionTestComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
