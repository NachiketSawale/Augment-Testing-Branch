//import { ComponentFixture, TestBed } from '@angular/core/testing';

//import { UiCommonScriptEditorComponent } from './script-editor.component';

// TODO: fix test cases
describe('ScriptEditorComponent', () => {
	//let component: UiCommonScriptEditorComponent;
	//let fixture: ComponentFixture<UiCommonScriptEditorComponent>;

	/*
	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiCommonScriptEditorComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(UiCommonScriptEditorComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/

	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
});
