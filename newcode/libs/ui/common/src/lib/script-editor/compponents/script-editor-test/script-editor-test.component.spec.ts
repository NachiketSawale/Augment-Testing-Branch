import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UiCommonScriptEditorTestComponent } from './script-editor-test.component';

describe('ScriptEditorTestComponent', () => {
	let component: UiCommonScriptEditorTestComponent;
	let fixture: ComponentFixture<UiCommonScriptEditorTestComponent>;

	/*beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [ScriptEditorTestComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(ScriptEditorTestComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/

	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
});
