import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LookupHostComponent } from './lookup-host.component';

describe('LookupHostComponent', () => {
  /*
  let component: LookupHostComponent<object>;
  let fixture: ComponentFixture<LookupHostComponent<object>>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ LookupHostComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(LookupHostComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });


  it('should create', () => {
    expect(component).toBeTruthy();
  });
   */

  // TODO: replace with actual test cases
  it('is successful', () => {
    expect(true).toBeTruthy();
  });
});
