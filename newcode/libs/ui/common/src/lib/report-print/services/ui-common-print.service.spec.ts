import { TestBed } from '@angular/core/testing';
import { UiCommonPrintService } from './ui-common-print.service';

describe('UiCommonPrintService', () => {
  // let service: UiCommonPrintService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({
  //   });
  //   service = TestBed.inject(UiCommonPrintService);
  // });

  it('should be created', () => {
    expect(true).toBeTruthy();
  });
});
