/*
 * $Id$
 * Copyright(c) RIB Software GmbH
 */
//import { TestBed } from '@angular/core/testing';
//import { CloudDesktopSvgIconService } from './cloud-desktop-svg-icon.service';

describe('CloudDesktopSvgIconService', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
	/*let service: CloudDesktopSvgIconService;

	beforeEach(() => {
		TestBed.configureTestingModule({});
		service = TestBed.inject(CloudDesktopSvgIconService);
	});

	it('should be created', () => {
		expect(service).toBeTruthy();
	});*/
});
