/*
 * $Id$
 * Copyright(c) RIB Software GmbH
 */

import { SvgImageDirective } from './svg-image.directive';

describe('SvgimageDirective', () => {
	it('should create an instance', () => {
		//const directive = new SvgimageDirective();
		//expect(directive).toBeTruthy();
	});
});
