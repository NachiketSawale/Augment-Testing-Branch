/*
 * $Id$
 * Copyright(c) RIB Software GmbH
 */

//import { ComponentFixture, TestBed } from '@angular/core/testing';

//import { UiMainFrameSessionInfoHeaderComponent } from './session-info-header.component';

describe('UiMainFrameSessionInfoHeaderComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
/*	let component: UiMainFrameSessionInfoHeaderComponent;
	let fixture: ComponentFixture<UiMainFrameSessionInfoHeaderComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiMainFrameSessionInfoHeaderComponent],
		}).compileComponents();
	});

	beforeEach(() => {
		fixture = TestBed.createComponent(UiMainFrameSessionInfoHeaderComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
