import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UiContainerSystemLayoutSplitterComponent } from './layout-splitter.component';

describe('LayoutSplitterComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});

	/*
	let component: UiContainerSystemLayoutSplitterComponent;
	let fixture: ComponentFixture<UiContainerSystemLayoutSplitterComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiContainerSystemLayoutSplitterComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(UiContainerSystemLayoutSplitterComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});
	 */
});
