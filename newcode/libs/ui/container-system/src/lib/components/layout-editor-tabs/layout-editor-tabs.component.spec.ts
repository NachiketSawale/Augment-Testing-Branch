//import { ComponentFixture, TestBed } from '@angular/core/testing';
//import { UiContainerSystemLayoutEditorTabsComponent } from './layout-editor-tabs.component';

describe('LayoutEditorTabsComponent', () => {
//	let component: UiContainerSystemLayoutEditorTabsComponent;
//	let fixture: ComponentFixture<UiContainerSystemLayoutEditorTabsComponent>;

	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});

	/*
	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiContainerSystemLayoutEditorTabsComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(UiContainerSystemLayoutEditorTabsComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});
	 */
});
