//import { ComponentFixture, TestBed } from '@angular/core/testing';
//import { UiContainerSystemLayoutEditorViewComponent } from './layout-editor-view.component';

describe('LayoutEditorViewComponent', () => {
//	let component: UiContainerSystemLayoutEditorViewComponent;
//	let fixture: ComponentFixture<UiContainerSystemLayoutEditorViewComponent>;

	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});

	/*
	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [UiContainerSystemLayoutEditorViewComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(UiContainerSystemLayoutEditorViewComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});
	 */
});
