/*
 * $Id$
 * Copyright(c) RIB Software GmbH
 */
import { HttpClientTestingModule } from '@angular/common/http/testing';
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UiContainerSystemNavbarComponent } from './navbar.component';

describe('UiContainerSystemNavbarComponent', () => {
	/*let component: UiContainerSystemNavbarComponent;
	let fixture: ComponentFixture<UiContainerSystemNavbarComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			imports:[HttpClientTestingModule],
			declarations: [UiContainerSystemNavbarComponent],
			providers:[]
		}).compileComponents();
	});

	beforeEach(() => {
		fixture = TestBed.createComponent(UiContainerSystemNavbarComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});*/

	it('should create', () => {
		expect(true).toBeTruthy();
	});
});
