/*
 * Copyright(c) RIB Software GmbH
 */

describe('ProcurementPricecomparisonCompareSettingGridLayoutComponent', () => {
  /*let component: CompareSettingGridLayoutComponent;
  let fixture: ComponentFixture<CompareSettingGridLayoutComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [CompareSettingGridLayoutComponent],
    }).compileComponents();

    fixture = TestBed.createComponent(CompareSettingGridLayoutComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });*/

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
