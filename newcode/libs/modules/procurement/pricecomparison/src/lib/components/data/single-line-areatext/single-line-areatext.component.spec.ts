/*
 * Copyright(c) RIB Software GmbH
 */

describe('ProcurementPricecomparisonSingleLineAreatextComponent', () => {
	/*let component: SingleLineAreatextComponent;
	let fixture: ComponentFixture<SingleLineAreatextComponent>;

	beforeEach(async () => {
	  await TestBed.configureTestingModule({
		 declarations: [SingleLineAreatextComponent],
	  }).compileComponents();

	  fixture = TestBed.createComponent(SingleLineAreatextComponent);
	  component = fixture.componentInstance;
	  fixture.detectChanges();
	});

	it('should create', () => {
	  expect(component).toBeTruthy();
	});*/

	it('should create', () => {
		expect(true).toBeTruthy();
	});
});
