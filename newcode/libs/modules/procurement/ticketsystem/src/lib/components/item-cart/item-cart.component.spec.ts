import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ProcurementTicketSystemItemCartComponent } from './item-cart.component';

describe('ItemCartComponent', () => {
  // TODO: replace with actual test cases
  it('is successful', () => {
    expect(true).toBeTruthy();
  });
  /*let component: ProcurementTicketSystemItemCartComponent;
  let fixture: ComponentFixture<ProcurementTicketSystemItemCartComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ProcurementTicketSystemItemCartComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ProcurementTicketSystemItemCartComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
