import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ProcurementTicketSystemCommoditySearchComponent } from './commodity-search.component';

describe('CommoditySearchComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});

	/*
	let component: ProcurementTicketSystemCommoditySearchComponent;
	let fixture: ComponentFixture<ProcurementTicketSystemCommoditySearchComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [ProcurementTicketSystemCommoditySearchComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(ProcurementTicketSystemCommoditySearchComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});
	 */
});
