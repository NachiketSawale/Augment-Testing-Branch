/*
 * Copyright(c) RIB Software GmbH
 */
/**
 * The document's columns which to be used in project document container
 */
export interface IColumnConfig {
    documentField: string;
    dataField: string;
    readOnly: boolean;
}