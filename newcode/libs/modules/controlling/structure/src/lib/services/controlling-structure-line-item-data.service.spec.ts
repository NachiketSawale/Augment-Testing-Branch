/**
 * Copyright(c) RIB Software GmbH
 */

import { TestBed } from '@angular/core/testing';

import { ControllingStructureLineItemDataService } from './controlling-structure-line-item-data.service';

describe('ControllingStructureLineItemDataService', () => {
	// let service: ControllingStructureLineItemDataService;

	// beforeEach(() => {
	// 	TestBed.configureTestingModule({});
	// 	service = TestBed.inject(ControllingStructureLineItemDataService);
	// });

	it('should be created', () => {
		expect(true).toBeTruthy();
	});
});
