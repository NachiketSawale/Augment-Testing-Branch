/**
 * Copyright(c) RIB Software GmbH
 */

import { TestBed } from '@angular/core/testing';

import { ControllingVersionDataService } from './controlling-versions-data.service';

describe('ControllingVersionsDataService', () => {
	// let service: ControllingVersionDataService;

	// beforeEach(() => {
	// 	TestBed.configureTestingModule({});
	// 	service = TestBed.inject(ControllingVersionDataService);
	// });

	it('should be created', () => {
		expect(true).toBeTruthy();
	});
});
