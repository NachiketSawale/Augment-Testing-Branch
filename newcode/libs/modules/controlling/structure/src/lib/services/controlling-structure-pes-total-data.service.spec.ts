/**
 * Copyright(c) RIB Software GmbH
 */

import { TestBed } from '@angular/core/testing';

import { ControllingStructurePesTotalDataService } from './controlling-structure-pes-total-data.service';

describe('ControllingStructurePesTotalDataService', () => {
	// let service: ControllingStructurePesTotalDataService;

	// beforeEach(() => {
	// 	TestBed.configureTestingModule({});
	// 	service = TestBed.inject(ControllingStructurePesTotalDataService);
	// });

	it('should be created', () => {
		expect(true).toBeTruthy();
	});
});
