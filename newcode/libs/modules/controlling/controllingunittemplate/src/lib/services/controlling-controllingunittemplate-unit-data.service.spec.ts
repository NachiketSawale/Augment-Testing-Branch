/**
 * Copyright(c) RIB Software GmbH
 */

import { ControllingControllingunittemplateUnitDataService } from './controlling-controllingunittemplate-unit-data.service';

describe('ControllingControllingunittemplateUnitDataService', () => {
	// let service: ControllingControllingunittemplateUnitDataService;

	// beforeEach(() => {
	// 	TestBed.configureTestingModule({});
	// 	service = TestBed.inject(ControllingControllingunittemplateUnitDataService);
	// });

	it('should be created', () => {
		expect(true).toBeTruthy();
	});
});
