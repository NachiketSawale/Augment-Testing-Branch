/**
 * Copyright(c) RIB Software GmbH
 */

import { ControllingControllingunittemplateDataService } from './controlling-controllingunittemplate-data.service';

describe('ControllingControllingunittemplateDataService', () => {
	let service: ControllingControllingunittemplateDataService;

	// TODO: add unit tests
	// beforeEach(() => {
	// 	TestBed.configureTestingModule({});
	// 	service = TestBed.inject(ControllingControllingunittemplateDataService);
	// });

	// TODO: replace with unit test
	it('should be created', () => {
		expect(true).toBeTruthy();
	});
});
