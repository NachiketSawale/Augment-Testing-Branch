/*
 * $Id$
 * Copyright(c) RIB Software GmbH
 */
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { BasicsClerkForPackageDetailComponent } from './basics-clerk-for-package-detail.component';

describe('BasicsClerkForPackageDetailComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
/*	let component: BasicsClerkForPackageDetailComponent;
	let fixture: ComponentFixture<BasicsClerkForPackageDetailComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [ BasicsClerkForPackageDetailComponent ]
		})
			.compileComponents();
	});

	beforeEach(() => {
		fixture = TestBed.createComponent(BasicsClerkForPackageDetailComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
