/*
 * $Id$
 * Copyright(c) RIB Software GmbH
 */
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { BasicsClerkAbsenceProxyDetailComponent } from './basics-clerk-absence-proxy-detail.component';

describe('BasicsClerkAbsenceProxyDetailComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
/*	let component: BasicsClerkAbsenceProxyDetailComponent;
	let fixture: ComponentFixture<BasicsClerkAbsenceProxyDetailComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [ BasicsClerkAbsenceProxyDetailComponent ]
		})
			.compileComponents();
	});

	beforeEach(() => {
		fixture = TestBed.createComponent(BasicsClerkAbsenceProxyDetailComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
