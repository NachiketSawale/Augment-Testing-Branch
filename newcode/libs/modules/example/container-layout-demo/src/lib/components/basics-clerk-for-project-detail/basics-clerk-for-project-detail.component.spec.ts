/*
 * $Id$
 * Copyright(c) RIB Software GmbH
 */
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { BasicsClerkForProjectDetailComponent } from './basics-clerk-for-project-detail.component';

describe('BasicsClerkForProjectDetailComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
/*	let component: BasicsClerkForProjectDetailComponent;
	let fixture: ComponentFixture<BasicsClerkForProjectDetailComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [ BasicsClerkForProjectDetailComponent ]
		})
			.compileComponents();
	});

	beforeEach(() => {
		fixture = TestBed.createComponent(BasicsClerkForProjectDetailComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
