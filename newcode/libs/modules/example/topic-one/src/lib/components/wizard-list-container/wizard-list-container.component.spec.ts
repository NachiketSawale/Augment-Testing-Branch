import { ComponentFixture, TestBed } from '@angular/core/testing';

import { WizardListContainerComponent } from './wizard-list-container.component';

describe('WizardListContainerComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
/*	let component: WizardListContainerComponent;
	let fixture: ComponentFixture<WizardListContainerComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [WizardListContainerComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(WizardListContainerComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
