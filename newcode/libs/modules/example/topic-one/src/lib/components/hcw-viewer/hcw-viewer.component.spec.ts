/*
 * $Id: container-group.component.ts 47506 2022-07-22 13:52:29Z haagf $
 * Copyright(c) RIB Software GmbH
 */

import { ComponentFixture, TestBed } from '@angular/core/testing';

import { HcwViewerComponent } from './hcw-viewer.component';

describe('HcwViewerComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
/*	let component: HcwViewerComponent;
	let fixture: ComponentFixture<HcwViewerComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [HcwViewerComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(HcwViewerComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});*/
});
