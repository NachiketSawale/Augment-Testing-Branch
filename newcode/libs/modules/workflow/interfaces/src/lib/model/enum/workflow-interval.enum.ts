/**
 * Specifies the interval for workflow items.
 */
export enum WorkflowInterval {
    /**
     * Specifies the interval in which workflow client tasks are reloaded.
     */
    ShortTaskReloadInterval = 6000
}