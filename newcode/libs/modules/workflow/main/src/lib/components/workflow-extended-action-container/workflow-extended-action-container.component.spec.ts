/*
 * Copyright(c) RIB Software GmbH
 */

import { ComponentFixture, TestBed } from '@angular/core/testing';
import { WorkflowExtendedActionContainerComponent } from './workflow-extended-action-container.component';

describe('ModulesWorkflowMainWorkflowExtendedActionContainerComponent', () => {
  //let component: WorkflowExtendedActionContainerComponent;
  //let fixture: ComponentFixture<WorkflowExtendedActionContainerComponent>;

  //beforeEach(async () => {
   //  await TestBed.configureTestingModule({
   //    declarations: [WorkflowExtendedActionContainerComponent],
   //  }).compileComponents();

    //fixture = TestBed.createComponent(WorkflowExtendedActionContainerComponent);
    //component = fixture.componentInstance;
    //fixture.detectChanges();
  //});

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
