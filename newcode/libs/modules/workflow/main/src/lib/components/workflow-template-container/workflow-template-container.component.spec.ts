//import { ComponentFixture, TestBed } from '@angular/core/testing';
//import { WorkflowTemplateContainerComponent } from './workflow-template-container.component';

describe('WorkflowTemplateContainerComponent', () => {
	/**
	let component: WorkflowTemplateContainerComponent;
	let fixture: ComponentFixture<WorkflowTemplateContainerComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [WorkflowTemplateContainerComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(WorkflowTemplateContainerComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});
	 **/
	it('should create', () => {
		expect(true).toBeTruthy();
	});
});
