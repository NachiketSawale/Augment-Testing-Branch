import { TestBed } from '@angular/core/testing';

import { WorkflowTemplateContextService } from './workflow-template-context.service';

describe('WorkflowTemplateContextDataService', () => {
	// let service: WorkflowTemplateContextDataService;

	// beforeEach(() => {
	// 	TestBed.configureTestingModule({});
	// 	service = TestBed.inject(WorkflowTemplateContextDataService);
	// });

	it('should be created', () => {
		expect(true).toBeTruthy();
	});
});
