import { TestBed } from '@angular/core/testing';

import { WorkflowPinnedIdService } from './workflow-pinned-id.service';

describe('WorkflowPinnedIdService', () => {
	let service: WorkflowPinnedIdService;

	// beforeEach(() => {
	// 	TestBed.configureTestingModule({});
	// 	service = TestBed.inject(WorkflowPinnedIdService);
	// });

	it('should be created', () => {
		expect(true).toBeTruthy();
	});
});
