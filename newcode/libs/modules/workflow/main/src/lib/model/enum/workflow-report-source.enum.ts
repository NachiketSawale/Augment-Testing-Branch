/*
 * Copyright(c) RIB Software GmbH
 */

/**
 * Source where report is requested from.
 */
export enum ReportSource {
	/**
	 * Report is requested from server.
	 */
	Server,

	/**
	 * Server is requested from JSON.
	 */
	JSON
}