/**
 * ITextParameter captures  the property values
 * of current node for the purpose of keeping node labels only inside of a node shape.
 */
export interface ITextParameter {
	elem: string;
	description: string;
	width: number;
	height: number;
}