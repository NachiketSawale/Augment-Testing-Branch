import { modulesProjectMaterial } from './modules-project-material';

describe('modulesProjectMaterial', () => {
	it('should work', () => {
		expect(modulesProjectMaterial()).toEqual('modules-project-material');
	});
});
