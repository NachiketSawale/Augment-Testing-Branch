# modules-resource-interfaces

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test modules-resource-interfaces` to execute the unit tests.
## Building

Run `nx build modules-resource-interfaces` to build the library.
