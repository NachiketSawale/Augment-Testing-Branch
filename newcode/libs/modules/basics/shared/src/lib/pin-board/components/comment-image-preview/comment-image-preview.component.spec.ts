/*
 * Copyright(c) RIB Software GmbH
 */

import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CommentImagePreviewComponent } from './comment-image-preview.component';

describe('CommentImagePreviewComponent', () => {
  let component: CommentImagePreviewComponent;
  let fixture: ComponentFixture<CommentImagePreviewComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [CommentImagePreviewComponent],
  //   }).compileComponents();
  //
  //   fixture = TestBed.createComponent(CommentImagePreviewComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
