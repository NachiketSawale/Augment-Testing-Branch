/*
 * Copyright(c) RIB Software GmbH
 */

import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BasicsSharedCommentEditorComponent } from './comment-editor.component';

describe('BasicsSharedCommentEditorComponent', () => {
  let component: BasicsSharedCommentEditorComponent;
  let fixture: ComponentFixture<BasicsSharedCommentEditorComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [BasicsSharedCommentEditorComponent],
  //   }).compileComponents();
  //
  //   fixture = TestBed.createComponent(BasicsSharedCommentEditorComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
