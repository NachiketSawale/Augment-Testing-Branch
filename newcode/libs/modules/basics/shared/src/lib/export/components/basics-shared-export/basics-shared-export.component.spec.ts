import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BasicsSharedExportComponent } from './basics-shared-export.component';

describe('BasicsSharedExportComponent', () => {
	// let component: BasicsSharedExportComponent;
	// let fixture: ComponentFixture<BasicsSharedExportComponent>;
	//
	// beforeEach(async () => {
	//   await TestBed.configureTestingModule({
	//     imports: [BasicsSharedExportComponent]
	//   }).compileComponents();
	//
	//   fixture = TestBed.createComponent(BasicsSharedExportComponent);
	//   component = fixture.componentInstance;
	//   fixture.detectChanges();
	// });

	it('should create', () => {
		expect(true).toBeTruthy();
	});
});
