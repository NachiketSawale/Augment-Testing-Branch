import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BasicsSharedMaterialLookupComponent } from './material-lookup.component';

describe('MaterialLookupComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});

	/*
	let component: BasicsSharedMaterialLookupComponent;
	let fixture: ComponentFixture<BasicsSharedMaterialLookupComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [BasicsSharedMaterialLookupComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(BasicsSharedMaterialLookupComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});
	 */
});
