import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BasicsSharedMaterialItemDescriptionComponent } from './material-item-description.component';

describe('MaterialItemDescriptionComponent', () => {
  // TODO: replace with actual test cases
  it('is successful', () => {
    expect(true).toBeTruthy();
  });
  /*let component: MaterialItemDescriptionComponent;
  let fixture: ComponentFixture<MaterialItemDescriptionComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ MaterialItemDescriptionComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(MaterialItemDescriptionComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
