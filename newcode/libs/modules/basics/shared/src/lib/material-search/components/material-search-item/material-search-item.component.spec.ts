import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BasicsSharedMaterialSearchItemComponent } from './material-search-item.component';
import { PlatformCommonModule} from '@libs/platform/common';
import {HttpClientModule} from '@angular/common/http';

describe('MaterialSearchItemComponent', () => {
  // TODO: replace with actual test cases
  it('is successful', () => {
    expect(true).toBeTruthy();
  });

  /*let component: BasicsSharedMaterialSearchItemComponent;
  let fixture: ComponentFixture<BasicsSharedMaterialSearchItemComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [HttpClientModule, PlatformCommonModule],
      declarations: [ BasicsSharedMaterialSearchItemComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(BasicsSharedMaterialSearchItemComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
