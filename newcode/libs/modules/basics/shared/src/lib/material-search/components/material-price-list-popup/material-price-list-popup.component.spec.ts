import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BasicsSharedMaterialPriceListPopupComponent } from './material-price-list-popup.component';

describe('BasicsSharedMaterialPriceListPopupComponent', () => {
  // TODO: replace with actual test cases
  it('is successful', () => {
    expect(true).toBeTruthy();
  });
  /*let component: MaterialPriceListPopupComponent;
  let fixture: ComponentFixture<MaterialPriceListPopupComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ MaterialPriceListPopupComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(MaterialPriceListPopupComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
