import { TestBed } from '@angular/core/testing';

import { BasicsSharedMaterialCreateSimilarMaterialService } from './material-create-similar-material.service';

describe('BasicsSharedMaterialSimilarService', () => {
  // TODO: replace with actual test cases
  it('is successful', () => {
    expect(true).toBeTruthy();
  });
  /*let service: BasicsSharedMaterialSimilarService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(BasicsSharedMaterialSimilarService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });*/
});
