import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BasicsSharedMaterialAlternativeListComponent } from './material-alternative-list.component';

describe('BasicsSharedMaterialAlternativeListComponent', () => {
  // TODO: replace with actual test cases
  it('is successful', () => {
    expect(true).toBeTruthy();
  });
  /*let component: BasicsSharedMaterialAlternativeListComponent;
  let fixture: ComponentFixture<BasicsSharedMaterialAlternativeListComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ BasicsSharedMaterialAlternativeListComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(BasicsSharedMaterialAlternativeListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
