import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PrintImportResultComponent } from './print-import-result.component';

describe('PrintImportResultComponent', () => {
	let component: PrintImportResultComponent;
	let fixture: ComponentFixture<PrintImportResultComponent>;

	// beforeEach(async () => {
	//   await TestBed.configureTestingModule({
	//     imports: [PrintImportResultComponent]
	//   }).compileComponents();
	//
	//   fixture = TestBed.createComponent(PrintImportResultComponent);
	//   component = fixture.componentInstance;
	//   fixture.detectChanges();
	// });

	it('should create', () => {
		expect(true).toBeTruthy();
	});
});
