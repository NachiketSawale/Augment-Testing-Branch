/*
 * Copyright(c) RIB Software GmbH
 */

import { BasicsSharedSyncDocumentsToBim360DialogComponent } from './sync-documents-to-bim360-dialog.component';

describe('BasicsSharedSyncDocumentsToBim360DialogComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
	/*
  let component: BasicsSharedSyncDocumentsToBim360DialogComponent;
  let fixture: ComponentFixture<BasicsSharedSyncDocumentsToBim360DialogComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [BasicsSharedSyncDocumentsToBim360DialogComponent],
    }).compileComponents();

    fixture = TestBed.createComponent(BasicsSharedSyncDocumentsToBim360DialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
 */
});
