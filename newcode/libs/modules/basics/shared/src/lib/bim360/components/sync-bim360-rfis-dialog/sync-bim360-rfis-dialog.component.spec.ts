/*
 * Copyright(c) RIB Software GmbH
 */

import { SyncBim360RfisDialogComponent } from './sync-bim360-rfis-dialog.component';

describe('SyncBim360RfisDialogComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
	/*
  let component: SyncBim360RfisDialogComponent;
  let fixture: ComponentFixture<SyncBim360RfisDialogComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [SyncBim360RfisDialogComponent],
    }).compileComponents();

    fixture = TestBed.createComponent(SyncBim360RfisDialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
	 */
});
