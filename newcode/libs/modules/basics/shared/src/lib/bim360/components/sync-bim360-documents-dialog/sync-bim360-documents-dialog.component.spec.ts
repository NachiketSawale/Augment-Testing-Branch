/*
 * Copyright(c) RIB Software GmbH
 */

import { BasicsSharedSyncBim360DocumentsDialogComponent } from './sync-bim360-documents-dialog.component';

describe('BasicsSharedSyncBim360DocumentsDialogComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});

	/*
  let component: BasicsSharedSyncBim360DocumentsDialogComponent;
  let fixture: ComponentFixture<BasicsSharedSyncBim360DocumentsDialogComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [BasicsSharedSyncBim360DocumentsDialogComponent],
    }).compileComponents();

    fixture = TestBed.createComponent(BasicsSharedSyncBim360DocumentsDialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
