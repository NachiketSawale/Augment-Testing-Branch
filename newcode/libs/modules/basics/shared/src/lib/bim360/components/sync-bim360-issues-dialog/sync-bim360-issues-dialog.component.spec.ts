/*
 * Copyright(c) RIB Software GmbH
 */

import { SyncBim360IssuesDialogComponent } from './sync-bim360-issues-dialog.component';

describe('SyncBim360IssuesDialogComponent', () => {
	// TODO: replace with actual test cases
	it('is successful', () => {
		expect(true).toBeTruthy();
	});
	/*
	let component: SyncBim360IssuesDialogComponent;
	let fixture: ComponentFixture<SyncBim360IssuesDialogComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			declarations: [SyncBim360IssuesDialogComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(SyncBim360IssuesDialogComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});
	*/
});
