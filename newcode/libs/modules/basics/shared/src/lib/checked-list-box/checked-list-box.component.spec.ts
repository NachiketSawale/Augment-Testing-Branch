import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BasicsSharedCheckedListBoxComponent } from './checked-list-box.component';

describe('BasicsSharedCheckedListBoxComponent', () => {
	// TODO: Should be implemented in the future.
  // let component: BasicsSharedCheckedListBoxComponent;
  // let fixture: ComponentFixture<BasicsSharedCheckedListBoxComponent>;
  //
  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [BasicsSharedCheckedListBoxComponent]
  //   }).compileComponents();
  //
  //   fixture = TestBed.createComponent(BasicsSharedCheckedListBoxComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
