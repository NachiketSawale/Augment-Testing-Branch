//import { ComponentFixture, TestBed } from '@angular/core/testing';

//import { UiCommonStatusChangeEditorComponent } from './status-change-editor.component';

describe('BasicsSharedStatusChangeEditorComponent', () => {
	/*  TODO need to find solution how to test JueryUI related component
	let component: UiCommonStatusChangeEditorComponent;
	let fixture: ComponentFixture<UiCommonStatusChangeEditorComponent>;


	beforeEach(async () => {
		 await TestBed.configureTestingModule({
			  declarations: [UiCommonStatusChangeEditorComponent, jQuery],
		 }).compileComponents();

		 fixture = TestBed.createComponent(UiCommonStatusChangeEditorComponent);
		 component = fixture.componentInstance;
		 fixture.detectChanges();
	});
	*/
	it('should create', () => {
		//expect(component).toBeTruthy();
	});
});
