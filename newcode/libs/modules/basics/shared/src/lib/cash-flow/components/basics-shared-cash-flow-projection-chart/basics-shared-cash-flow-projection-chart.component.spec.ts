import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BasicsSharedCashFlowProjectionChartComponent } from './basics-shared-cash-flow-projection-chart.component';

describe('BasicsSharedCashFlowProjectionChartComponent', () => {
	// TODO: Should be implemented in the future.
  // let component: BasicsSharedCashFlowProjectionChartComponent;
  // let fixture: ComponentFixture<BasicsSharedCashFlowProjectionChartComponent>;
  //
  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [BasicsSharedCashFlowProjectionChartComponent]
  //   }).compileComponents();
  //
  //   fixture = TestBed.createComponent(BasicsSharedCashFlowProjectionChartComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
