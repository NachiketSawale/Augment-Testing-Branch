import { TestBed } from '@angular/core/testing';

import { BasicsSharedCreateService } from './create.service';
import { HttpClientModule } from '@angular/common/http';

describe('BasicsSharedCreateService', () => {
	// TODO: Should be implemented in the future.
	// let service: BasicsSharedCreateService;
	//
	// beforeEach(() => {
	// 	TestBed.configureTestingModule({
	// 		imports: [
	// 			HttpClientModule
	// 		]
	// 	});
	// 	service = TestBed.inject(BasicsSharedCreateService);
	// });

	it('should be created', () => {
		expect(true).toBeTruthy();
	});
});
