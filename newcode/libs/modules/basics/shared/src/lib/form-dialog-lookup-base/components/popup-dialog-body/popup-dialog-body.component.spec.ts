/*
 * $Id$
 * Copyright(c) RIB Software GmbH
 */

import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BasicsSharedPopupDialogBodyComponent } from './popup-dialog-body.component';
import { HttpClientModule } from '@angular/common/http';
import { UiCommonModule } from '@libs/ui/common';

describe('CustomPopupDialogBodyComponent', () => {
	// TODO: Should be implemented in the future.
	// let component: BasicsSharedPopupDialogBodyComponent<object, object>;
	// let fixture: ComponentFixture<BasicsSharedPopupDialogBodyComponent<object, object>>;
	//
	// beforeEach(async () => {
	// 	await TestBed.configureTestingModule({
	// 			declarations: [BasicsSharedPopupDialogBodyComponent],
	// 			imports: [
	// 				HttpClientModule,
	// 				UiCommonModule
	// 			]
	// 		})
	// 		.compileComponents();
	//
	// 	fixture = TestBed.createComponent(BasicsSharedPopupDialogBodyComponent);
	// 	component = fixture.componentInstance;
	// 	fixture.detectChanges();
	// });

	it('should create', () => {
		expect(true).toBeTruthy();
	});
});
