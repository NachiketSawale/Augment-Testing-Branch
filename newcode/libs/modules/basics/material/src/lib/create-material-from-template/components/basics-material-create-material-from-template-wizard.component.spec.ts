/*
 * Copyright(c) RIB Software GmbH
 */

describe('BasicsMaterialCreateMaterialFromTemplateWizardComponent', () => {
	/*let component: BasicsMaterialCreateMaterialFromTemplateWizardComponent;
	let fixture: ComponentFixture<BasicsMaterialCreateMaterialFromTemplateWizardComponent>;

	beforeEach(async () => {
		await TestBed.configureTestingModule({
			imports: [BasicsMaterialCreateMaterialFromTemplateWizardComponent],
		}).compileComponents();

		fixture = TestBed.createComponent(BasicsMaterialCreateMaterialFromTemplateWizardComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});*/

	it('should create', () => {
		expect(true).toBeTruthy();
	});
});
