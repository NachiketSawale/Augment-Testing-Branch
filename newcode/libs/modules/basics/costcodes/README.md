# modules-basics-costcodes

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build modules-basics-costcodes` to build the library.

## Running unit tests

Run `nx test modules-basics-costcodes` to execute the unit tests via [Jest](https://jestjs.io).
