/*
 * Copyright(c) RIB Software GmbH
 */

export interface IControllingCostCodes {
	/*
	 * showAssignToChildren
	 */
    ShowAssignToChildren?: boolean;

	/*
	 * showOverwrite
	 */

	ShowOverwrite?: boolean;

	/*
	 * showPreserveSelection
	 */
    ShowPreserveSelection: boolean;

	/*
	 * isOverwrite
	 */
    IsOverwrite: boolean;
	/*
	 * isAssignToChildren
	 */

	IsAssignToChildren?: boolean;

	/*
	 * isPreserveSelection
	 */
	IsPreserveSelection?: boolean;
}
