/*
 * Copyright(c) RIB Software GmbH
 */

describe('ProcurementPricecomparisonCreateContractWizardViewComponent', () => {
	/*let component: CreateContractWizardViewComponent;
	let fixture: ComponentFixture<CreateContractWizardViewComponent>;

	beforeEach(async () => {
	  await TestBed.configureTestingModule({
		 declarations: [CreateContractWizardViewComponent],
	  }).compileComponents();

	  fixture = TestBed.createComponent(CreateContractWizardViewComponent);
	  component = fixture.componentInstance;
	  fixture.detectChanges();
	});

	it('should create', () => {
	  expect(component).toBeTruthy();
	});*/

	it('should create', () => {
		expect(true).toBeTruthy();
	});
});
