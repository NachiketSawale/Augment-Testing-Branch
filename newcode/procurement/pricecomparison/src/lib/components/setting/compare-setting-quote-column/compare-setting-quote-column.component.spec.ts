/*
 * Copyright(c) RIB Software GmbH
 */

describe('ProcurementPricecomparisonCompareSettingQuoteColumnComponent', () => {
  /*let component: CompareSettingQuoteColumnComponent;
  let fixture: ComponentFixture<CompareSettingQuoteColumnComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [CompareSettingQuoteColumnComponent],
    }).compileComponents();

    fixture = TestBed.createComponent(CompareSettingQuoteColumnComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });*/

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
