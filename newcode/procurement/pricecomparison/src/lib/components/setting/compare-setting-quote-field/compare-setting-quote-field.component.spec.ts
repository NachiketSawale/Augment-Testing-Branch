/*
 * Copyright(c) RIB Software GmbH
 */

describe('ProcurementPricecomparisonCompareSettingQuoteFieldComponent', () => {
  /*let component: CompareSettingQuoteFieldComponent;
  let fixture: ComponentFixture<CompareSettingQuoteFieldComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [CompareSettingQuoteFieldComponent],
    }).compileComponents();

    fixture = TestBed.createComponent(CompareSettingQuoteFieldComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });*/

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
