/*
 * Copyright(c) RIB Software GmbH
 */

import {ComponentFixture, TestBed} from '@angular/core/testing';
import {SelectItemVariantDialogComponent} from './select-item-variant-dialog.component';

describe('SelectItemVariantDialogComponent', () => {
	/* let component: SelectItemVariantDialogComponent;
	let fixture: ComponentFixture<SelectItemVariantDialogComponent>;

	beforeEach(async () => {
	  await TestBed.configureTestingModule({
		declarations: [SelectItemVariantDialogComponent],
	  }).compileComponents();

	  fixture = TestBed.createComponent(SelectItemVariantDialogComponent);
	  component = fixture.componentInstance;
	  fixture.detectChanges();
	}); */

	it('should create', () => {
		expect(true).toBeTruthy();
		// expect(component).toBeTruthy();
	});
});
