# modules-procurement-stock

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build modules-procurement-stock` to build the library.

## Running unit tests

Run `nx test modules-procurement-stock` to execute the unit tests via [Jest](https://jestjs.io).
